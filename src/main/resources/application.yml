spring:
  application:
    name: MovieService
  datasource:
    url: jdbc:mysql://localhost:3306
    username: root
    password: Dktran897

server:
  address: 0.0.0.0
  port: 8082
  error:
    include-exception: true
    include-message: always

logging:
  file:
    name: ./MovieService.log